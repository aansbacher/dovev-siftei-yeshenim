import { createClient } from '@supabase/supabase-js'
import fs from 'fs'
import path from 'path'

function parseEnv(f) {
  return fs.readFileSync(f, 'utf8').split(/\r?\n/).reduce((e, l) => {
    const c = l.trim()
    if (!c || c.startsWith('#')) return e
    const [k, ...r] = c.split('=')
    e[k.trim()] = r.join('=').trim()
    return e
  }, {})
}
const env = parseEnv(path.resolve('c:/Users/aansb/dovev-siftei-yeshenim/.env.local'))
const sb = createClient(env.VITE_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY)

const updates = [
  // 11781 — רבי דוד בידרמן מלעלוב (72)
  // story: קצר מ-100 תווים, צריך סיפור מלא
  // torah: ביבליוגרפי גרידא, צריך תורה אמיתית
  {
    id: 11781,
    story: 'ר\' דוד מלעלוב שקד שנים ארוכות על תעניות ופרישות, מבקש להגיע לשלמות בעבודת ה\'. כשהגיע לבסוף אל החוזה מלובלין, שאלו הרבי: "מי הוא הגרוע ביותר בעירך?" ענה ר\' דוד: "רופא פלוני — פורק עול ומחלל שבת בפרהסיה." מיד הסב ממנו החוזה את פניו ולא נתן לו שלום. ר\' דוד חזר לביתו נפעם ומבולבל. בהדרגה הבין: כל שנות הפרישות לא הועילו כלל — שכן עדיין לא למד לאהוב כל יהודי, אפילו החוטא. מאז שינה את דרכו לגמרי. קירב פושעים, ירד לשווקים ולבתי המרזח, וחיפש בכל יהודי את ניצוץ הקדושה שבו. החוזה מלובלין אמר עליו: "רק כשיבוא משיח ידעו בני העולם את ערכו."',
    torah: '"אין עבודת ה\' שלמה אם לבו של אדם אינו פתוח לכל יהודי — גם למרוחק ביותר. לא הפרישות מכשרת את האדם, אלא האהבה והקרבה." — ר\' דוד מלעלוב. לימד שכל יהודי נושא בתוכו ניצוץ אלוקי חי, וחובתו של הצדיק היא לחפש אותו ולהדליקו מחדש — לא לסתמו בגינוי.'
  },

  // 11746 — רבי יצחק אייזיק חבר (72)
  // torah: רשימת כותרות ספרים, לא תורה
  {
    id: 11746,
    torah: '"תורת הנסתר ותורת הנגלה — אחת הן, ומשלימות זו את זו. מי שלומד הלכה בלי קבלה — כמי שיש לו גוף בלי נשמה; מי שלומד קבלה בלי הלכה — נשמה ללא גוף." — ר\' יצחק אייזיק חבר, \'פתחי שערים\'. שיטתו שילבה את דרך הגר"א עם כתבי האר"י והרמח"ל, בהאמינו שרק מי שבקי בנגלה ובנסתר כאחד מגיע לאמת השלמה.'
  },

  // 12107 — ר' חיים בנבנישתי — כנסת הגדולה (72)
  // torah: ריק לגמרי
  {
    id: 12107,
    torah: '"כל דור וכל פוסק מוסיף חוליה בשלשלת ההלכה, ואין מאוחר מבטל את מוקדם — אלא מרחיב אותו." — ר\' חיים בנבנישתי, \'כנסת הגדולה\'. חיבורו הגדול ליקט את פסקי כל הגדולים מהבית יוסף ועד דורו, מתוך אמונה שכל תשובה הלכתית היא חלק ממשמעות ה\' המתגלה בכל דור מחדש.'
  },
]

let passed = 0, failed = 0
for (const u of updates) {
  const { id, ...fields } = u
  const { error } = await sb.from('tzaddikim').update(fields).eq('id', id)
  if (error) { console.error(`✗ ${id}: ${error.message}`); failed++ }
  else { process.stdout.write(`✓${id} `); passed++ }
}
console.log(`\n\nתורה+סיפורים — עודכנו: ${passed} | נכשלו: ${failed}`)
