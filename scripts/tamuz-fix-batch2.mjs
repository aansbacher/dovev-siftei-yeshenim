import { createClient } from '@supabase/supabase-js'
import fs from 'fs'
import path from 'path'

function parseEnv(f) {
  return fs.readFileSync(f, 'utf8').split(/\r?\n/).reduce((e, l) => {
    const c = l.trim()
    if (!c || c.startsWith('#')) return e
    const [k, ...r] = c.split('=')
    e[k.trim()] = r.join('=').trim()
    return e
  }, {})
}
const env = parseEnv(path.resolve('.env.local'))
const supabase = createClient(env.VITE_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY)

let passed = 0, failed = 0

// ===== יום 20 תמוז — ניקוי =====
// נשמר: 11280 (נתן נטע הנובר, הועבר מיום 10 בבאץ' 1), 11295 (אברהם יצחק בלוך), 11972 (קצות השלחן)
// מועבר: 8 רשומות לימים הנכונים שלהן
// נמחק: 26 רשומות שגויות

const moves = [
  { id: 11305, hebrew_day: 22, reason: 'שלמה מקרלין — bio: "כ"ב תמוז ה\'תקנ"ב"' },
  { id: 11306, hebrew_day: 22, reason: 'דוד מקוצק — שמו: "כ"ב בתמוז תרל"ג"' },
  { id: 11307, hebrew_day: 22, reason: 'נחמיה אלתר — שמו: "כ"ב בתמוז תש"ב"' },
  { id: 11308, hebrew_day: 22, reason: 'רפאל הכהן קוק — bio: "כ"ב בתמוז תשל"א"' },
  { id: 11309, hebrew_day: 27, reason: 'שמואל רוזובסקי — דירשו יום כז (תשל"ט)' },
  { id: 11318, hebrew_day: 28, reason: 'אלעזר וייסבלום מליז\'נסק — שמו: "כ"ח בתמוז תקס"ו"' },
  { id: 11319, hebrew_day: 28, reason: 'משה טייטלבוים מאוהל (ישמח משה) — bio: "כ"ח תמוז ה\'תר"א"' },
  { id: 11320, hebrew_day: 26, reason: 'שלמה גאנצפריד (קיצור שו"ע) — דירשו יום כו (תרמ"ו)' },
]

console.log('מעביר 8 רשומות מיום 20 לימים הנכונים...')
for (const { id, hebrew_day, reason } of moves) {
  const { error } = await supabase.from('tzaddikim').update({ hebrew_day }).eq('id', id)
  if (error) { console.error(`✗ ${id}: ${error.message}`); failed++ }
  else { process.stdout.write(`→${id}(${hebrew_day}) `); passed++ }
}

const deleteIds = [
  11296, // לוי יצחק בנדר מנדבורנה — לא ברשימת דירשו
  11297, // אברהם דוד רוזנטל — לא ברשימת דירשו
  11299, // יהודה הכהן שאקו — לא ברשימת דירשו
  11300, // שלמה שמאמא — bio שגויה (על אבן גבירול!)
  11301, // רחמים נאהורי — לא ברשימת דירשו
  11302, // רפאל משה אלבז — לא ברשימת דירשו
  11303, // יום טוב ידיד הלוי — לא ברשימת דירשו
  11304, // מנוח הנדל בן שמריה — לא ברשימת דירשו
  11310, // יהודה עמיטל — לא ברשימת דירשו
  11311, // אלעזר אבוחצירא — נפטר ד' אב, לא תמוז
  11312, // נתן שפיגלגלס — לא ברשימת דירשו
  11313, // שלמה גולדמן מזוועהיל — לא ברשימת דירשו
  11314, // יעקב חי ציון עדס — לא ברשימת דירשו
  11315, // משה אבירמאט — bio שגויה (על דוד שלוש)
  11316, // יעקב מאיר עבאדי — bio שגויה (על יעקב שאול קצין)
  11317, // שמואל שאול עידאן — bio שגויה (על משה עידאן)
  11321, // יעקב שאול אלישר — לא ברשימת דירשו
  11322, // חיים פרידלנדר — לא ברשימת דירשו
  11324, // ישעיה משולם זוסיא מצ'רנוביל — לא ברשימת דירשו
  11325, // שלמה אליעזר טנא — לא ברשימת דירשו
  11326, // אברהם הרופא פורטלאונה — לא ברשימת דירשו
  11327, // אליהו בעל שם — לא ברשימת דירשו
  11328, // אברהם גרודזינסקי — לא ברשימת דירשו
  11329, // עלוואן שמעון אבידני — לא ברשימת דירשו
  11330, // יוחנן הסנדלר — לא ברשימת דירשו (ונפטר כנראה בחודש אחר)
  11331, // מאיר ביקייאם — לא ברשימת דירשו
]

console.log(`\nמוחק ${deleteIds.length} רשומות שגויות מיום 20 תמוז...`)
for (const id of deleteIds) {
  const { error } = await supabase.from('tzaddikim').delete().eq('id', id)
  if (error) { console.error(`✗ מחיקה ${id}: ${error.message}`); failed++ }
  else { process.stdout.write(`✗${id} `); passed++ }
}

console.log(`\n\nתמוז גל תיקון 2 — עברו: ${passed} | נכשלו: ${failed}`)
console.log(`
יום 20 אחרי ניקוי: 3 רשומות — 11280 (נתן נטע הנובר), 11295 (אברהם יצחק בלוך), 11972 (קצות השלחן)
הועברו מיום 20:
  יום 22: 11305 (שלמה מקרלין), 11306 (דוד מקוצק), 11307 (נחמיה אלתר), 11308 (רפאל קוק)
  יום 26: 11320 (שלמה גאנצפריד)
  יום 27: 11309 (שמואל רוזובסקי)
  יום 28: 11318 (אלעזר מליז\'נסק), 11319 (ישמח משה)
`)
