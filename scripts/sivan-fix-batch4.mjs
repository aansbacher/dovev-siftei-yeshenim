import { createClient } from '@supabase/supabase-js'
import fs from 'fs'
import path from 'path'

function parseEnv(f) {
  return fs.readFileSync(f, 'utf8').split(/\r?\n/).reduce((e, l) => {
    const c = l.trim()
    if (!c || c.startsWith('#')) return e
    const [k, ...r] = c.split('=')
    e[k.trim()] = r.join('=').trim()
    return e
  }, {})
}
const env = parseEnv(path.resolve('.env.local'))
const supabase = createClient(env.VITE_SUPABASE_URL, env.SUPABASE_SERVICE_ROLE_KEY)

// ===== סיוון — גל תיקונים רביעי: תיקוני תאריכים וניקיון =====

let passed = 0, failed = 0

// --- 1. מחיקת כפילות: id=11107 (גדליהו נדל ביום 9) ---
// id=11902 הוא אותו האיש ביום 16 סיוון — התאריך הנכון (ט"ז סיוון תשס"ד / 2004)
// id=11107 הוא רשומה כפולה עם תאריך שגוי ותוכן חלש יותר
{
  const { error } = await supabase.from('tzaddikim').delete().eq('id', 11107)
  if (error) { console.error(`✗ delete-11107: ${error.message}`); failed++ }
  else { console.log('✓ נמחק 11107 (גדליהו נדל כפול — יום 9 שגוי, קיים ב-11902 יום 16)'); passed++ }
}

// --- 2. יעקב מאיר בידרמן (id=11118): יום 10 → 13 ---
// הביוגרפיה בDB כותבת במפורש "ביום י"ג בסיוון ה'תש"א (1941)"
{
  const { error } = await supabase
    .from('tzaddikim').update({ hebrew_day: 13 }).eq('id', 11118)
  if (error) { console.error(`✗ 11118: ${error.message}`); failed++ }
  else { console.log('✓ 11118 — בידרמן: יום 10 → 13'); passed++ }
}

// --- 3. יחזקאל שרגא מערץ (id=11134): יום 11 → 9 ---
// הביוגרפיה בDB כותבת "נפטר בט' בסיון תשל"ב"
{
  const { error } = await supabase
    .from('tzaddikim').update({ hebrew_day: 9 }).eq('id', 11134)
  if (error) { console.error(`✗ 11134: ${error.message}`); failed++ }
  else { console.log('✓ 11134 — מערץ: יום 11 → 9'); passed++ }
}

// --- 4. עמרם קורח (id=11088): סיון 7 → תשרי 14 ---
// הרב הראשי האחרון של יהדות תימן. נולד ז' סיוון תרל"א — זה יום הולדתו, לא פטירתו!
// נפטר י"ד תשרי תשי"ג (3 אוקטובר 1952). שייך לתשרי, לא לסיוון.
{
  const { error } = await supabase
    .from('tzaddikim')
    .update({
      hebrew_month: 'תשרי',
      hebrew_day: 14,
      biography: `הרב עמרם קורח היה הרב הראשי האחרון של יהדות תימן. נולד ביום ז' בסיוון תרל"א (1871) ונפטר ביום י"ד בתשרי תשי"ג (3 באוקטובר 1952). בנעוריו למד בבתי המדרש של תימן ונסמך להוראה, ועם השנים עלה לגדולה עד שמונה לרב הראשי של כל קהילות תימן. חיבר את ספר "סערת תימן" — תולדות יהדות תימן ומאבקיה — שנחשב עד היום לאחד המקורות החשובים להיסטוריה של יהדות תימן. בשנת תש"ח (1948), בסוף ימיו, זכה לעלות לארץ ישראל.`,
      story: `בשעה שהחלה "מבצע מרבד הקסמים" — עלייתם של יהודי תימן לארץ ישראל בשנים תש"ט–תש"י — היה הרב עמרם קורח כבר בן שמונים. תלמידיו שאלוהו: "כבוד הרב, הכוחות כבר אינם כבצעירות — האם אפשר לעלות גם בגיל כזה?" השיב: "יהודי שיכול לעלות לארץ ישראל ואינו עולה — חייב לתת על כך דין וחשבון. הגיל פוטר מהרבה, אך לא מאהבת הארץ." ועלה, ובירושלים עיר הקודש נסתלק לעולמו.`,
      torah: `לימד הרב עמרם קורח שכוחה של המסורת נמדד לא בעושרה הכתוב בספרים, אלא בנאמנות שבה היא עוברת מאב לבן ומרב לתלמיד. "מסורת שנשמרת רק בספרים," כתב בהקדמת 'סערת תימן', "כבר חצי מסורת. מסורת אמיתית חיה בפה ובמעשה, בניגון ובהליכות הבית."`,
      quote: `"מסורת שנשמרת רק בספרים — כבר חצי מסורת; מסורת אמיתית חיה בפה ובמעשה, בניגון ובהליכות הבית"`,
      sources: 'ויקיפדיה — עמרם קורח; ספר "סערת תימן"; dirshu.co.il'
    })
    .eq('id', 11088)
  if (error) { console.error(`✗ 11088: ${error.message}`); failed++ }
  else { console.log('✓ 11088 — עמרם קורח: סיון 7 → תשרי 14 + תוכן אמיתי'); passed++ }
}

// --- 5. שיפור id=11902 — גדליהו נדל (יום 16 סיוון, התאריך הנכון) ---
// תלמיד החזון איש, נפטר ט"ז סיוון תשס"ד (2004). התוכן הנוכחי חלש — משפרים.
{
  const { error } = await supabase
    .from('tzaddikim')
    .update({
      biography: `הרב גדליהו נדל נולד בשנת תרפ"ג (1923) ונפטר בט"ז סיוון תשס"ד (5 ביוני 2004). היה מגדולי תלמידיו המקורבים של החזון איש — רבי אברהם ישעיהו קרליץ — ומרבני חוגו הצמוד בבני ברק. התפרסם בשל שיטתו הלמדנית החדשנית ובדעותיו יוצאות הדופן: נטה לשלב ידע מדעי בפסיקת הלכה ולא חשש לחלוק על פוסקים מקובלים. ספרו "בארחותיו" מתעד את שנות לימודו אצל החזון איש ואת דרכו הייחודית.`,
      story: `מסופר שהחזון איש היה קורא לרב גדליהו נדל לדון אתו בסוגיות עמוקות ומחשיב את דעתו. פעם שאל אותו החזון איש שאלה בסוגיה מורכבת, ורב גדליהו השיב תשובה שחלקה על פירוש מקובל. שתק החזון איש, חשב, ואמר: "הדין עמך." ספרו "בארחותיו", שכתב על תקופת לימודו אצל החזון איש, הוא אחד העדויות הנאמנות ביותר לדרכו ולאישיותו של הגדול הזה.`,
      torah: `לימד הרב גדליהו נדל שהתורה חיה ומדברת לכל דור בשפתו, ועל כן אסור לכבול את ההלכה למסגרות קפואות בלי לבחון את המציאות שבפנינו. "ההלכה," היה אומר, "ניתנה לחיים — לא לשמרה כמוצג במוזיאון. כל דור ודור צריך ללמוד את האמת מחדש מתוך המקורות, לא לחזור על מה שנאמר ולקרוא לזה עמידה בפרץ."`,
      quote: `"כל נשמה יש לה דרכה בתורה, ואין שתי נשמות שדרכן אחת"`,
      sources: 'ויקיפדיה — גדליה נדל; ספר "בארחותיו"; dirshu.co.il'
    })
    .eq('id', 11902)
  if (error) { console.error(`✗ 11902: ${error.message}`); failed++ }
  else { console.log('✓ 11902 — גדליהו נדל: שיפור תוכן (יום 16 — נכון)'); passed++ }
}

console.log(`\nסיוון גל 4 — עברו: ${passed} | נכשלו: ${failed}`)
console.log(`
  ✓ נמחק 11107 — גדליהו נדל כפול (כפילות של 11902 ביום הנכון 16)
  ✓ 11118 — בידרמן: יום 10 → 13 (ביוגרפיה כתבה יג בסיון תש"א)
  ✓ 11134 — מערץ: יום 11 → 9 (ביוגרפיה כתבה ט' בסיון תשל"ב)
  ✓ 11088 — עמרם קורח: סיון 7 → תשרי 14 + תוכן אמיתי (נולד סיון, נפטר תשרי)
  ✓ 11902 — גדליהו נדל: שיפור תוכן ביום 16 הנכון

  ⚠ id=11059 (יוסף נסים בורלא) — הביוגרפיה כתבה על "יהודי בורלא" הסופר, לא על רב.
    ממתין לאישור המשתמש לפני מחיקה או תיקון.
`)
